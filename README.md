# FEATERA 行事曆排程系統 — Railway PostgreSQL 雲端版

## Railway 必要變數
在 `featera-calendar → Variables` 確認：

- `DATABASE_URL=${{Postgres.DATABASE_URL}}`
- `ADMIN_USER=Featera`（可自行更改）
- `ADMIN_PASSWORD=請設定強密碼`
- `SESSION_SECRET=請設定一串長隨機字串`

`DATABASE_URL` 已存在且正確時不要重複新增。

## 雲端同步
- `server.js` 啟動時自動建立 `featera_calendar_state` 資料表。
- 管理員登入後自動下載 PostgreSQL 最新資料。
- 管理員修改並儲存時，資料會自動同步到 PostgreSQL。
- 訪客登入後自動讀取 PostgreSQL 最新資料，沒有寫入權限。
- 「雲端」按鈕仍可手動立即上傳或重新下載。

## 第一次部署
1. 將本專案內容推送到 GitHub repository 根目錄。
2. Railway 會依 `package.json` 執行 `npm start`。
3. 第一次啟動會自動建表。
4. 管理員第一次登入時，如果雲端尚無資料，會將目前裝置資料初始化到 PostgreSQL。

## 健康檢查
部署後開啟 `/api/health`，正常應回傳：
`{"ok":true,"database":true}`

## 安全
管理員密碼不再寫在瀏覽器端程式中。請在 Railway Variables 管理帳密，不要將資料庫密碼寫入 `app.js`。

## v3 視覺更新
- 月曆輸出版面改為 FEATERA 2026/9 範例風格：白底、黑色表格線、週六週日紅字。
- 日期改為左上角單一數字，跨月維持空白。
- 公司資訊維持右上、Logo 左上、標題置中，分公司電話與地址維持頁尾雙欄。
- 行程文字仍保留逐行字級、顏色、粗體、斜體、底線與對齊設定。


## v4 Excel 排程資料庫整合
- 整合 `課程行事曆安排(1).xlsx` 的 37 筆講師資料與 66 筆主持人資料。
- 納入說明會資格、支援區域、年資、課程推薦與課程頻率提示。
- `reference.json` 會隨 PostgreSQL cloud payload 保存。

## v5 參考版面調整
- 月曆輸出版面依 FEATERA 2026 年 9 月公司行事曆重新校正：白底、粗黑外框、細黑內框、週末紅字。
- 標題顯示改為「2026 年 9 月行事曆」格式，Logo／大標題／營業時間比例重新配置。
- 日期靠左上，月曆格內容密度與行距調整，分公司聯絡資訊改為雙欄緊湊配置。
- 保留 PostgreSQL 雲端同步、排程資料庫、三個月檢查、文字格式設定、PNG 匯出與手機操作功能。

## v6 官方 9 月行事曆匯出樣式
- PNG 匯出改為 A4 橫式固定比例（1584×1120 CSS px，再由 html2canvas 2x 輸出）。
- 星期列統一白底黑字，星期六、星期日為紅字。
- 月曆格統一白底、黑色外框與細黑內框，不再使用綠色/藍綠色星期底或淡綠日期列。
- 日期改為格子左上角，第一筆行程首行自動避開日期位置，更接近官方行事曆排版。
- 行程文字仍保留每行自訂顏色、大小、粗體、斜體、底線與對齊功能；匯出時不強制覆蓋使用者設定的文字顏色。
- `style.css` 與 `app.js` 加入 v6 cache-busting query，更新 GitHub / Railway 後可避免瀏覽器沿用舊版 CSS。


## v7 版面配色功能
- 新增「星期 / 日期配色」設定。
- 星期一至星期日可分別設定背景色與文字色，背景支援透明。
- 日期欄位可設定背景色/透明、一般/週六/週日/國定假日文字色、字體大小與偏左/置中/偏右。
- 國定假日保留系統預設紅色，但管理員可自行改色或停用假日專用色。
- 所有外觀設定會寫入既有雲端 state，PNG 匯出與不同裝置同步套用相同設定。
